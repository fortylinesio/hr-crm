package io.fortylines.hrcrm.pageable;

public class VacancyPageRequest extends PageRequest {
    public VacancyPageRequest(int limit, int offset, String sortBy) {
        super(limit, offset, sortBy);
    }
}
